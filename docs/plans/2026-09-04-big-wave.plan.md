# Big Wave — perf / LSP / editors / lift / transpile / vision

> Developer directive, 2026-09-04: *"i want a very big wave ! very very big ! with
> perf/lsp/editors/lift/transpile/vision ! a continuous/autonomous very long and fruitful
> session !"* — autonomous, `advisor()` per gate, panel only at the wave's frozen close.

## Decisions Log
- [2026-09-04 22:40] AGREED: the wave runs as six independently-committable lanes, each green
  before the next starts, so an interrupted session never leaves a broken tree.
- [2026-09-04 22:40] AGREED: Lanes E and F are POINTERS at readiness-plan steps 19 and 3, never
  second copies of them — Invariant 19 forbids a parallel roadmap doc.
- [2026-09-04 22:40] AGREED: a diagnostic code with no reachable emit site is RETIRED, not
  fixtured — `codes_total` may legitimately fall. A test asserting an unreachable string is
  coverage theater.
- [2026-09-04 22:40] AGREED: Lane D fixes the helper-count drift by RATCHETING the `//!` header
  against the registry (doc name lists + the three declared counts), not by correcting prose —
  the previous "the ratchet asserts this" claim was false and prose alone would re-rot.

## Formal Plan

| Lane | Surface | Deliverable |
|---|---|---|
| **D** | transpile | `helper_buckets.rs`: 7 missing names added, counts 68/105/173 → 71/116/187, and a NEW test asserting the header lists+counts against `HELPER_BUCKETS`. Sabotage: delete one `//!` name → must red. |
| **C** | lift | `lift_from` rows for this wave's natives so PHP→Phorj round-trips them (`wordwrap`, `soundex`, `str_word_count`-family, …); charset/foldAccents stay empty BY RULING (no liftable PHP counterpart without an ini extension). |
| **A** | lsp + editors | `signatureHelp` (named by Invariant 17 and ABSENT), plus `inlayHint`/`semanticTokens` as reach; VS Code + JetBrains updated in the SAME change. |
| **B** | vision | the 55 diagnostic codes with zero assertions → 314/314, closing Invariant 17's 100% rule for diagnostics. Clustered: enum-backed 13, DB-hydrate 7, named-arg 4, hook 4, format 3, concurrency 6, rest 18. |
| **F** | vision | parity-% recompute — **the readiness plan's step 3 is canonical**; this row is a pointer, not a second copy (Invariant 19). |
| **E** | perf | DEC-333 — **the readiness plan's step 19 is canonical**; this row is a pointer. Precondition: per-core `mpstat` idle, else the verdict is recorded OWED, never a number (NO-HIDDEN-LOSS). |

## Status
<!-- progress-block v1 -->
| # | Step | Size | State | Evidence | Files |
|---|------|------|-------|----------| ------- |
| 1 | Lane D — helper-count drift RATCHETED (doc lists + counts asserted against `HELPER_BUCKETS`) | M | doing | - | src/transpile/helper_buckets.rs |
| 2 | Lane C — `lift_from` rows for the wave's new natives | M | todo | - | src/native/registry_modules/* src/lift/* |
| 3 | Lane A — LSP `signatureHelp` + editors same-change | L | todo | - | src/lsp/* editors/* |
| 4 | Lane B — 55 unasserted diagnostic codes → 314/314 | L | todo | - | src/**/tests/* tests/* |
| 5 | Lane F — parity-% recompute (drives readiness step 3; that row is canonical) | S | todo | - | docs/research/full-audit/raw/M-gap-matrix.md |
| 6 | Lane E — DEC-333 perf (drives readiness step 19; that row is canonical) | L | todo | - | bench/* src/* |
<!-- /progress-block -->
### Blocked
### Needs input
### Needs research
### Fragile
### Known issues
