// Conformance: checked exceptions — `throws`, `throw`, `try`/`catch`/`finally`.
package Main;

import Core.Output;

class BadInputError implements Error {
    constructor(public string message) {}
}

function checked(int n): int throws BadInputError {
    if (n < 0) {
        throw new BadInputError("negative: {n}");
    }
    return n * 2;
}

#[Entry]
function main(): void {
    try {
        Output.printLine("ok {checked(5)}");
        Output.printLine("ok {checked(-1)}");
    } catch (BadInputError e) {
        Output.printLine("caught {e.message}");
    } finally {
        Output.printLine("done");
    }
}
