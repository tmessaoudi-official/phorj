type error at 10:5: method `k` overrides `Base`'s `k` but returns `string`, which is not assignable to the overridden return type `int`
    function k(): string {
    ^
  [E-OVERRIDE-SIG]
  hint: make `k`'s return type `int` or a subtype of it
